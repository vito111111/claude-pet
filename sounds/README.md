# 提示音

宠物在某个会话**完成（Stop）**时会播放一次提示音。

把任意一个 `.wav` 文件放到本目录、命名为 **`done.wav`** 即可，无需改代码：

```
sounds/done.wav
```

仓库**不附带**音频文件（避免分发受版权保护的音效）。若 `sounds/done.wav` 不存在，
程序会自动回退：系统 `tada.wav` → 合成提示音，因此不放也能正常运行。

> 想要"咳咳"咳嗽音可运行 `python make_cough.py` 在本目录生成一个 `done.wav`。
