# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS / editor
Thumbs.db
.DS_Store
.vscode/
.idea/

# 运行时状态（不应入库）
pet_status/
*.heartbeat

# 提示音：自带的是本机专有音频，不入库；用户自行放置（见 sounds/README.md）
sounds/*.wav
_release_notes.md

# 健身引导：第三方版权素材（任天堂官网动作照），不入库，运行时本地生成/放置
linefit/model_png/
linefit/frames/

# MediaPipe 模型文件较大，不入库（运行时按 linefit/README.md 下载）
*.task

# 健身引导 QA 蒙太奇/临时图(含任天堂参考素材合成),不入库
linefit/data/_tmp/
linefit/data/*_montage.png
linefit/data/qa_*.png
